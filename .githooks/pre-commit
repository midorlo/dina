#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(git rev-parse --show-toplevel)"
cd "$REPO_ROOT/frontend"

echo "[pre-commit] Running lint:check in ./frontend"

if ! command -v npm >/dev/null 2>&1; then
  echo "[pre-commit] npm not found in PATH" >&2
  exit 1
fi

npm run lint:check

